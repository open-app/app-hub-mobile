import Dat from 'dat-node'
import { reject } from 'async';

// syncDat: syncs dat from hash
// setStoragePath: sets dats storage path on fs

export const datSync = (datHash) => new Promise((resolve, reject) => {

})